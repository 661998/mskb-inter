<div class="row links">
  {% for item in items %}
	<div class="col-sm-12 text-center mb-1">
	  <a class="btn btn-warning w-100 py-3" href="{{ item.url }}">{{ item.text }}</a>
	</div>
  {% endfor %}
</div>
